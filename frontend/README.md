# Frontend (React)

This is a minimal React frontend scaffold for the leave application. It expects the backend at `../backend` running on `http://localhost:4000`.

To run:

```bash
cd leaveapplication/frontend
npm install
npm start
```
